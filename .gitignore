# Arduino IDE build artifacts
build/
*.bak
*.bin
*.hex
*.elf
*.map

# Local / generated config (none should ever contain committed secrets,
# but ignore any local overrides a builder might create)
firmware/**/secrets.h
firmware/**/secret.h
firmware/**/local_config.h

# IDE / editor
.vscode/
.idea/
.claude/
*.swp

# macOS
.DS_Store

# Local working notes (internal, not for the public repo)
HANDOFF.md
