# Repo settings — copy/paste

Quick reference for the GitHub "About" panel and repo settings. This file is
just a convenience note; delete it any time.

## Description (About → ⚙️)

> Standalone ESP32-S3 indoor air & environment monitor (BME688 + HDC3022) with
> a captive-portal WiFi setup and a local web dashboard — no Home Assistant
> required. Reachable at http://airbox.local.

## Topics (About → ⚙️ → Topics)

```
esp32  esp32-s3  qt-py  bme688  hdc3022  air-quality  environment-monitor
arduino  ssd1306  captive-portal  web-dashboard  iot  ota
```

## Website field (optional)

Leave blank, or set it to `http://airbox.local` as a hint (it isn't a public URL).

## Suggested settings to confirm

- **Visibility:** Public.
- **License:** GitHub should auto-detect **MIT** from `LICENSE`.
- **Features:** keep **Issues** on (bug reports). **Discussions** optional.
- **Actions:** none used — nothing to enable.
- **Collaborators:** only add someone if you want them to *push*; reading and
  cloning a public repo needs no access.

## Acknowledgment

The README's **Acknowledgments** section states plainly that the code was
AI-written (Anthropic's Claude) under the owner's direction — so it's clear the
owner didn't hand-code it. It's also recorded in the commit history via
`Co-Authored-By: Claude ...` trailers (GitHub shows co-authors).
